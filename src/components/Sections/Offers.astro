---
import Section from "../Section.astro";
import { getBarbers } from "../../services/BarbersServices";
import { getSession } from "auth-astro/server";

const barbers: Barbers[] = (await getBarbers()) as Barbers[];
const session = await getSession(Astro.request);
---

<Section id="Ofertas" styles="bg-gray-100 py-14 md:py-14 lg:px-14 ">
  <div class="container mx-auto">
    <h2 class="text-3xl md:text-4xl font-bold text-gray-900 capitalize">
      Ofertas del día
    </h2>
  </div>
</Section>
